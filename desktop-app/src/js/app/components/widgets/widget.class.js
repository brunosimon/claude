(function(window)
{
    "use strict";

    APP.COMPONENTS.Widget = APP.CORE.Event_Emitter.extend(
    {
        options:
        {

        },

        /**
         * INIT
         */
        init: function(options)
        {
            this._super(options);
        },

        /**
         * START
         */
        start: function()
        {

        }
    });
})(window);
